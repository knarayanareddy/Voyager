## Description
Describe the changes introduced by this pull request.

## Checklist
- [ ] Code is lint-free (`npm run lint` exits with 0).
- [ ] Types check successfully (`npm run typecheck` exits with 0).
- [ ] No new `any` types or assertions are introduced. If any are absolutely necessary, they are isolated to test files and/or annotated with a detailed ESLint disable comment containing a rationale.
- [ ] All unit tests pass (`npm run test` passes).
- [ ] Code coverage meets the target thresholds (`npm run test:coverage` meets the 60% global coverage rule).
